meta:
  environment: jvm
  toolchain:
    name: maven
    version: 16
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: SpringServer-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
